pkgname=julia-lazy-git
pkgver=0.10.0.0.43.gd99849e
pkgrel=1
pkgdesc="Lazy.jl"
url="https://github.com/one-more-minute/Lazy.jl"
arch=('any')
license=('MIT')
makedepends=('git' 'julia-pkg-scripts')
# TODO version depends
depends=('julia' 'julia-macrotools' 'julia-compat')
provides=('julia-lazy')
source=(git://github.com/one-more-minute/Lazy.jl)
md5sums=('SKIP')

pkgver() {
  cd Lazy.jl

  git describe | sed -e 's/^[^0-9]*//' -e 's/-/.0./' -e 's/-/./g'
}

package() {
  cd Lazy.jl

  . julia-install-pkg.sh Lazy "${pkgdir}" "${pkgname}"
}
