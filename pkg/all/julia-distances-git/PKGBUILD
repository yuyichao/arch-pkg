pkgname=julia-distances-git
pkgver=0.3.0.0.4.gb283f53
pkgrel=1
pkgdesc="Distances.jl"
url="https://github.com/JuliaStats/Distances.jl"
arch=('any')
license=('MIT')
makedepends=('git' 'julia-pkg-scripts')
# TODO version depends
depends=('julia')
provides=('julia-distances')
source=(git://github.com/JuliaStats/Distances.jl)
md5sums=('SKIP')

pkgver() {
  cd Distances.jl

  git describe | sed -e 's/^[^0-9]*//' -e 's/-/.0./' -e 's/-/./g'
}

package() {
  cd Distances.jl

  . julia-install-pkg.sh Distances "${pkgdir}" "${pkgname}"
}
