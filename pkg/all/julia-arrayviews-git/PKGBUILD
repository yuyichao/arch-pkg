pkgname=julia-arrayviews-git
pkgver=0.6.4.0.12.g93e8039
pkgrel=1
pkgdesc="ArrayViews.jl"
url="https://github.com/JuliaLang/ArrayViews.jl"
arch=('any')
license=('MIT')
makedepends=('git' 'julia-pkg-scripts')
# TODO version depends
depends=('julia' 'julia-compat')
provides=('julia-arrayviews')
source=(git://github.com/JuliaLang/ArrayViews.jl)
md5sums=('SKIP')

pkgver() {
  cd ArrayViews.jl

  git describe | sed -e 's/^[^0-9]*//' -e 's/-/.0./' -e 's/-/./g'
}

package() {
  cd ArrayViews.jl

  . julia-install-pkg.sh ArrayViews "${pkgdir}" "${pkgname}"
}
